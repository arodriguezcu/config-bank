server:
  port: 8100
#  port: 80
  hostname: localhost
#  hostname: 192.168.0.106
    
spring:
  application:
    name: micro-bootcoin
  data:
    mongodb:
      database: db_bootcoin
#      uri: mongodb://${server.hostname}:12500/${spring.data.mongodb.database}
      uri: mongodb://arodriguezcu:WoZiG7Enr4sZXJsPA6J1yAICIAeGAzczurFJNm8k3q0apmZjMRPlAdHKqoA4vCnXvwLT40VxnskXjU6AwznsuA==@arodriguezcu.mongo.cosmos.azure.com:10255/${spring.data.mongodb.database}?ssl=true&replicaSet=globaldb&retrywrites=false&maxIdleTimeMS=120000&appName=@arodriguezcu@
  kafka:
    bootstrap-servers: ${kafka.server.hostname}:${kafka.server.port}

kafka:
  server:
    hostname: 20.94.188.219
    port: 9092

eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:80/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    hostname: az-eureka-server.azurewebsites.net

msg:
  error:
    registro:
      notfound.all: MONEDEROS NO ENCONTRADOS 
      notfound: MONEDERO NO IDENTIFICADO
      notfound.create: MONEDERO NO SE PUDO CREAR
      if.exists: MONEDERO BOOTCOIN YA EXISTE
      phoneini.exists: NUMERO DE TELEFONO INICIAL NO EXISTE
      phonefin.exists: NUMERO DE TELEFONO FINAL NO EXISTE
      accountini.exists: NUMERO DE CUENTA INICIAL NO EXISTE
      accountfin.exists: NUMERO DE CUENTA FINAL NO EXISTE
      positive: MONTO DEBE SER POSITIVO
      exceed: MONTO EXCEDE EL SALDO DISPONIBLE      
      notfound.update: MONEDERO NO IDENTIFICADO PARA ACTUALIZAR
      notfound.delete: MONEDERO NO IDENTIFICADO PARA ELIMINAR
      wallet.delete: MONEDERO ELIMINADO
      request:      
        notfound.all: SOLICITUDES NO ENCONTRADOS 
        if.exists: SOLICITUD YA EXISTE
        exists: SOLICITUD NO EXISTE
      rate:      
        notfound.all: TIPO DE CAMBIO NO ENCONTRADOS

resilience4j:
  circuitbreaker:
    configs:
      default:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 2s
        failureRateThreshold: 50
        eventConsumerBufferSize: 10
        recordExceptions:
          - org.springframework.web.client.HttpServerErrorException
          - java.util.concurrent.TimeoutException
          - java.io.IOException
          - java.net.ConnectException
    instances:
      customerServiceCircuitBreaker:
        baseConfig: default
